/*
 Criar 2 arquivos 

 -o 1 ira exporta uma função chamada getFlag() [ arquivo flags.js]
 -que recebera um argumento do tipo String
 -essa função devera buscar dentro do array process.argv a flag desejada e retornar o valor da flag

 o 2 ira importar a função e passar a flag desejada

 iremos rodar no terminal o segundo arquivo passando as flags --name e --greting para que seja impresso no terminal a saudação e o nome da pessoa


 node greetings.js --name "Rafael" --greeting "Tudo bem?"
*/
